# Dependencies installed by Vercel's @vercel/python runtime for api/index.py.
# (uvicorn is intentionally omitted — Vercel provides the ASGI server.)
fastapi==0.115.0
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.10
openpyxl==3.1.5
PyJWT==2.9.0
# Postgres driver for production (Neon). DATABASE_URL uses postgresql+psycopg://
psycopg[binary]==3.2.3
